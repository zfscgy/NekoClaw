# 工具使用说明

工具签名会通过函数调用自动提供。
本文件记录不明显的约束与用法。

## exec — 安全限制

- 命令有可调超时（默认 60 秒）
- 危险命令会被拦截（如 rm -rf、format、dd、关机等）
- 输出截断在 10,000 字符
- 配置项 `restrictToWorkspace` 可将文件访问限制在工作区内

## cron — 定时提醒

- 用法请参阅 cron 相关技能文档。
